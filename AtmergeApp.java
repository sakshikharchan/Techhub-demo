import java.util.*;
public class AtmergeApp
{
	public static void main(String x[])
	
	{
		String first,second;
		Scanner xyz=new Scanner(System.in);
		System.out.println("Enter data in string ");
		first=xyz.nextLine();
		System.out.println("Enter data in secomd String");
		second =xyz.nextLine();
		Stringbuffer sb= new Stringbuffer();
		int limit=0,i=0,max;
		String str="";
		if(first.length()>second.length())
		{
			limit=second.length();
			
			max=first
		}
		
	}
}